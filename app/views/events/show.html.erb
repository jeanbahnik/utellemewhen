<h1><%= @event.name %></h1>
<ul>
  <% @event.event_dates.each do |event_date| %>
    <li>
      <%= event_date.name %>: <%= event_date.date.strftime("%B %d, %Y") %> <%= link_to "Edit", edit_event_date_url(event_date) %>
    </li>
  <% end %>
</ul>
